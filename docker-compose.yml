version: '3.8'
services:
  team-13-backend:
    image: konlawatit/team-13-backend:1.0
    ports:
      - 3013:3013
    command:
      sh -c 'npm i && npm start'
  team-13-frontend:
    image: konlawatit/team-13-frontend:1.0
    ports:
      - 8013:8013
  # nextjs:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   environment:
  #    - PORT=3001
  #   ports:
  #     - 3000:3000
  # nginx:
  #   build:
  #     context: ./nginx
  #     dockerfile: Dockerfile
  #   ports:
  #     - 80:80
